import { ContentManagerClient } from './ContentManagerClient';

export const metadata = {
  title: 'Content & Media Manager | Admin',
};

export default function MediaPage() {
  return <ContentManagerClient />;
}
